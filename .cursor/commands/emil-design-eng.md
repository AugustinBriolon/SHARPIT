# /emil-design-eng

Load **emil-design-eng** for design-engineering craft and interaction polish.

1. If `.agents/skills/emil-design-eng/SKILL.md` is missing, run `npx skills experimental_install`, then continue.
2. **Read** `.agents/skills/emil-design-eng/SKILL.md` first; follow it.
3. Precedence (always wins over skill defaults): `docs/design/*` + `PRODUCT.md` + `CORE_ARCHITECTURE.md`.
4. Use for polish/feel judgment inside SHARPIT’s DS — do not replace tokens, IA, or product law.
5. Apply to the current context (selection, @-files, or open UI work).
