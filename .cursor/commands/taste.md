# /taste

Alias of `/design-taste`. Load **design-taste-frontend**.

1. If `.agents/skills/design-taste-frontend/SKILL.md` is missing, run `npx skills experimental_install`, then continue.
2. **Read** `.agents/skills/design-taste-frontend/SKILL.md` first; follow it.
3. Precedence (always wins over skill / pack defaults): `docs/design/DESIGN_LANGUAGE.md`, `DESIGN_SYSTEM_PROMPT.md`, `INFORMATION_ARCHITECTURE.md`, `PRODUCT.md`, `CORE_ARCHITECTURE.md`.
4. Never override SHARPIT tokens, IA, or athlete-facing product UI with taste-pack aesthetics.
5. Apply to the current context (selection, @-files, or open UI work).
