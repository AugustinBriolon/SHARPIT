# /better-interface

Run a cross-cut interface critique/improve pass using the **better-interface** skill.

1. If `.agents/skills/better-interface/SKILL.md` is missing, run `npx skills experimental_install`, then continue.
2. **Read** `.agents/skills/better-interface/SKILL.md` first; follow it.
3. Precedence (always wins over skill defaults): `docs/design/DESIGN_LANGUAGE.md`, `DESIGN_SYSTEM_PROMPT.md`, `INFORMATION_ARCHITECTURE.md`, `PRODUCT.md`, `CORE_ARCHITECTURE.md`.
4. Keep French UI copy; refine within the existing DS — do not invent a new aesthetic.
5. Apply feedback to the current context (selection, @-files, or open UI work).
