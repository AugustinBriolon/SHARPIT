'use client';

/**
 * Legacy entry — Cap surface is the Objectifs body.
 * Kept so older imports keep resolving.
 */
export { GoalsCapView as GoalsView } from '@/components/goals/cap/goals-cap-view';
