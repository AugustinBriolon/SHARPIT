import { SettingsComingSoonPage } from '@/components/settings/settings-coming-soon-page';

export default function SettingsSourceRoutingPage() {
  return (
    <SettingsComingSoonPage
      blurb="Choisir, signal par signal, quelle app prime (FC, sommeil, poids…)."
      body="La préférence par classe de données existe déjà côté intégrations. Le routage fin par signal arrivera ici — pour l’instant, gère tes apps connectées."
      title="Routage des sources"
    />
  );
}
