/**
 * Public teaser layout — outside `(app)` shell (no tab bar, no glass chrome).
 * Matches onboarding / legal: light canvas, brand header owned by the funnel.
 */
export default function WelcomeLayout({ children }: { children: React.ReactNode }) {
  return children;
}
