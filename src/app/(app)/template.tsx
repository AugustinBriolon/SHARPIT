/**
 * App Router template — remounts on client navigations.
 * Intentionally no page-level fade: motion stays on small controls (pressable, etc.).
 */
export default function AppTemplate({ children }: { children: React.ReactNode }) {
  return children;
}
