import { CorpsHubSkeleton } from '@/components/corps/corps-hub-skeleton';

/** Legacy /corps redirect — keep chrome aligned with /biology. */
export default function Loading() {
  return <CorpsHubSkeleton />;
}
